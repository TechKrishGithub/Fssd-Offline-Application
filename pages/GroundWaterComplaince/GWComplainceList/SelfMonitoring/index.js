import { View, Text } from "react-native";
import React from "react";

const SelfMonitoringComplaince = (props) => {
  return (
    <View>
      <Text>SelfMonitoringComplaince</Text>
    </View>
  );
};

export default SelfMonitoringComplaince;
